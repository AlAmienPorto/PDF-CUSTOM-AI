{
  "buildCommand": "cd apps/web && npx pnpm install --no-frozen-lockfile && npx pnpm run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npx pnpm install --no-frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
